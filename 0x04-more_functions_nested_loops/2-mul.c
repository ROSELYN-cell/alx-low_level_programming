#include "main.h"
/**
 * mul - a function that multiplies two integers
 * @a:an integer
 * @b: an integer
 * Return: 0 on success and 1 on fail
 */
int mul(int a, int b)
{
	int n = a * b;

	return (n);
}
